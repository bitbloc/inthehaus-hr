name: Trigger HR Auto Alert

on:
  schedule:
    # สั่งให้ทำงานทุกๆ 10 นาที
    - cron: '*/10 * * * *'
  workflow_dispatch: # เพื่อให้เรากด Test รันเองได้ด้วย

jobs:
  ping-vercel:
    runs-on: ubuntu-latest
    steps:
      - name: Wake up Vercel API
        run: |
          # 🔴 แก้บรรทัดล่างนี้ เป็น URL เว็บของคุณนะครับ
          curl -X GET https://inthehaus-hr.vercel.app/api/cron/auto-remind